# create collection
db.createCollection("mongodb_database", { capped : true, size : 16384 })

# switch 
use mongodb_database

# show function
db.runCommand

# run command
db.runCommand({ dbstats : 1})

# show status
db.stats()


